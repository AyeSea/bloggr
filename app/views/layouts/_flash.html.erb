<div class="container-fluid container-flash">

	<% flash.each do |type, msg| %>
		<div class="alert <%= bootstrap_flash_class(type) %>">
			
			<div class="container">
			<%= msg %>
			</div>
			
		</div>
	<% end %>

</div>